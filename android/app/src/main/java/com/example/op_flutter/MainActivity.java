package com.example.op_flutterimport io.flutter.embedding.android.FlutterActivityimport io.flutter.embedding.engine.FlutterEngineimport io.flutter.plugin.common.MethodChannelimport com.pax.dal.IPrinterimport com.pax.neptunelite.api.NeptuneLiteUserclass MainActivity : FlutterActivity() {    private val CHANNEL = "com.example.op_flutter/printer"    override fun configureFlutterEngine(flutterEngine: FlutterEngine) {        super.configureFlutterEngine(flutterEngine)        MethodChannel(                flutterEngine.dartExecutor.binaryMessenger,                CHANNEL        ).setMethodCallHandler { call, result ->                when (call.method) {            "printStr" -> {                try {                    val text = call.argument<String>("text") ?: ""                    val param = call.argument<String>("param")                    // 获取DAL实例并调用IPrinter.printStr方法                    val neptuneLiteUser = NeptuneLiteUser.getInstance()                    val dal = neptuneLiteUser.getDal(this)                    val printer = dal.printer                    printer.init()                    printer.printStr('11111', param)                    val status = printer.start()                    result.success("打印成功，状态: $status")                } catch (e: Exception) {                    result.error("PRINT_ERROR", "打印失败: ${e.message}", null)                }            }                else -> result.notImplemented()        }        }    }}